# Archive

This directory is the default location for dormant or inactive material in the data storage layer.

Examples:

- archived notes
- inactive directions
- old project context
- raw imports
- large reference material
- historical snapshots

Do not expose archive content directly to the user data layer or prompt packages by default. Promote only focused excerpts, summaries, or reactivated notes when a current intent requires them.

{
  "schema": "abyss.change_set.v1",
  "id": "chg_r049_corrected_integrity_process_records_helper",
  "roadmap_id": "R049",
  "summary": "Extract process record integrity checks into a focused helper",
  "risk_level": "L2",
  "status": "proposed",
  "operations": [
    {
      "id": "op_001",
      "kind": "fs.replace_exact",
      "capability": "fs.write",
      "target": {
        "path": "abyss_cli/integrity.py"
      },
      "input": {
        "old_content": "def _check_required_structure(root: Path, messages: list[str]) -> bool:\n    ok = True\n    for rel in REQUIRED_DIRS:\n        if not (root / rel).exists():\n            ok = False\n            messages.append(f\"MISSING_DIR {rel}\")\n\n    for rel in REQUIRED_FILES:\n        if not (root / rel).exists():\n            ok = False\n            messages.append(f\"MISSING_FILE {rel}\")\n\n    return ok\n\n\ndef run_checks() -> tuple[bool, list[str]]:",
        "new_content": "def _check_required_structure(root: Path, messages: list[str]) -> bool:\n    ok = True\n    for rel in REQUIRED_DIRS:\n        if not (root / rel).exists():\n            ok = False\n            messages.append(f\"MISSING_DIR {rel}\")\n\n    for rel in REQUIRED_FILES:\n        if not (root / rel).exists():\n            ok = False\n            messages.append(f\"MISSING_FILE {rel}\")\n\n    return ok\n\n\ndef _check_process_records(root: Path, messages: list[str]) -> bool:\n    ok = True\n    for directory in [root / \"process\" / \"intents\", root / \"process\" / \"actions\", root / \"process\" / \"reviews\"]:\n        for path in list_records(directory):\n            try:\n                record = read_record(path)\n                if \"id\" not in record or \"schema\" not in record:\n                    ok = False\n                    messages.append(f\"INVALID_RECORD_FIELDS {path.relative_to(root)}\")\n            except Exception as exc:\n                ok = False\n                messages.append(f\"INVALID_JSON_YAML {path.relative_to(root)} {exc}\")\n\n    return ok\n\n\ndef run_checks() -> tuple[bool, list[str]]:"
      },
      "preconditions": [
        "old_content_matches_once"
      ],
      "rollback": {
        "strategy": "manual_revert_from_git_diff"
      }
    },
    {
      "id": "op_002",
      "kind": "fs.replace_exact",
      "capability": "fs.write",
      "target": {
        "path": "abyss_cli/integrity.py"
      },
      "input": {
        "old_content": "    ok = _check_required_structure(root, messages)\n\n    for directory in [root / \"process\" / \"intents\", root / \"process\" / \"actions\", root / \"process\" / \"reviews\"]:\n        for path in list_records(directory):\n            try:\n                record = read_record(path)\n                if \"id\" not in record or \"schema\" not in record:\n                    ok = False\n                    messages.append(f\"INVALID_RECORD_FIELDS {path.relative_to(root)}\")\n            except Exception as exc:\n                ok = False\n                messages.append(f\"INVALID_JSON_YAML {path.relative_to(root)} {exc}\")\n\n    runtime = runtime_root()",
        "new_content": "    ok = _check_required_structure(root, messages)\n    if not _check_process_records(root, messages):\n        ok = False\n\n    runtime = runtime_root()"
      },
      "preconditions": [
        "old_content_matches_once"
      ],
      "rollback": {
        "strategy": "manual_revert_from_git_diff"
      }
    },
    {
      "id": "check_001",
      "kind": "check.command",
      "capability": "process.check",
      "target": {
        "path": "system"
      },
      "input": {
        "command": "python -m compileall -q abyss_cli"
      },
      "preconditions": [
        "changes_applied_before_check"
      ],
      "rollback": {
        "strategy": "not_applicable"
      }
    },
    {
      "id": "check_002",
      "kind": "check.command",
      "capability": "process.check",
      "target": {
        "path": "system"
      },
      "input": {
        "command": "python -m abyss_cli check"
      },
      "preconditions": [
        "changes_applied_before_check"
      ],
      "rollback": {
        "strategy": "not_applicable"
      }
    },
    {
      "id": "check_003",
      "kind": "check.command",
      "capability": "process.check",
      "target": {
        "path": "system"
      },
      "input": {
        "command": "python -m abyss_cli summary --check"
      },
      "preconditions": [
        "changes_applied_before_check"
      ],
      "rollback": {
        "strategy": "not_applicable"
      }
    }
  ],
  "created_at": "2026-05-26T08:58:00+08:00",
  "no_action_executed": true
}
